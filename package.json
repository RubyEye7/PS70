{
  "name": "ps70-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server --files \"**/*\" --no-open --port 8000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}
